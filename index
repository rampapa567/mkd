<!DOCTYPE html>
<html lang="es-AR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Odontología Irigoyen — Tu consultorio en Villa Devoto</title>
<meta name="description" content="Odontología Irigoyen. Consultorio de barrio en Villa Devoto, CABA. Consultas, extracciones, radiografías, implantes y terapia láser. Turnos por WhatsApp.">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,300..900;1,9..144,300..900&family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
<style>

  :root{
    /* === Token system === */
    --clay:        #1f8a8a;   /* terracota — ladrillo visto de Floresta/Devoto */
    --clay-dark:   #156f6f;
    --sage:        #5aa37a;   /* verde salvia — clínico sin ser frío */
    --sage-dark:   #2f6f73;
    --cream:       #f4fbfb;   /* fondo cálido, no blanco hospital */
    --cream-deep:  #e7f6f6;
    --ink:         #2B231C;   /* casi negro, tibio */
    --ink-soft:    #5C534A;
    --line:        #cfe4e4;
    --white:       #FFFFFF;

    --serif: 'Fraunces', serif;
    --sans:  'Inter', sans-serif;
    --mono:  'JetBrains Mono', monospace;

    --max: 1140px;
  }

  *{ margin:0; padding:0; box-sizing:border-box; }

  html{ scroll-behavior:smooth; }

  body{
    background:var(--cream);
    color:var(--ink);
    font-family:var(--sans);
    font-size:16px;
    line-height:1.6;
    -webkit-font-smoothing:antialiased;
  }

  @media (prefers-reduced-motion: reduce){
    *{ animation-duration:0.001ms !important; transition-duration:0.001ms !important; }
  }

  a{ color:inherit; text-decoration:none; }
  img{ max-width:100%; display:block; }

  :focus-visible{
    outline:2.5px solid var(--clay);
    outline-offset:3px;
    border-radius:2px;
  }

  .wrap{
    max-width:var(--max);
    margin:0 auto;
    padding:0 28px;
  }

  /* === Texture: a faint brick-grid watermark, used sparingly === */
  .brick-texture{
    position:absolute;
    inset:0;
    background-image:
      repeating-linear-gradient(0deg, transparent, transparent 27px, rgba(181,85,45,0.05) 27px, rgba(181,85,45,0.05) 29px),
      repeating-linear-gradient(90deg, transparent, transparent 55px, rgba(181,85,45,0.05) 55px, rgba(181,85,45,0.05) 57px);
    pointer-events:none;
  }

  /* ============ HEADER ============ */
  header{
    position:sticky;
    top:0;
    z-index:50;
    background:rgba(250,244,236,0.92);
    backdrop-filter:blur(8px);
    border-bottom:1px solid var(--line);
  }
  .nav{
    display:flex;
    align-items:center;
    justify-content:space-between;
    padding:18px 28px;
  }
  .brand{
    display:flex;
    align-items:baseline;
    gap:10px;
  }
  .brand-mark{
    font-family:var(--serif);
    font-weight:600;
    font-size:1.45rem;
    letter-spacing:-0.01em;
    color:var(--clay-dark);
  }
  .brand-sub{
    font-family:var(--mono);
    font-size:0.68rem;
    letter-spacing:0.08em;
    text-transform:uppercase;
    color:var(--ink-soft);
  }
  .nav-links{
    display:flex;
    gap:32px;
    font-size:0.92rem;
    font-weight:500;
  }
  .nav-links a{
    color:var(--ink-soft);
    transition:color 0.2s;
    position:relative;
  }
  .nav-links a:hover{ color:var(--clay-dark); }
  .nav-cta{
    display:inline-flex;
    align-items:center;
    gap:8px;
    background:var(--clay);
    color:var(--white);
    padding:11px 20px;
    border-radius:7px;
    font-weight:600;
    font-size:0.9rem;
    transition:background 0.2s, transform 0.2s;
  }
  .nav-cta:hover{ background:var(--clay-dark); transform:translateY(-1px); }
  .nav-toggle{ display:none; }

  @media (max-width:860px){
    .nav-links{ display:none; }
    .brand-sub{ display:none; }
  }

  /* ============ HERO ============ */
  .hero{
    position:relative;
    overflow:hidden;
    padding:88px 0 76px;
    border-bottom:1px solid var(--line);
  }
  .hero-grid{
    display:grid;
    grid-template-columns:1.15fr 0.85fr;
    gap:56px;
    align-items:center;
    position:relative;
    z-index:1;
  }
  .eyebrow{
    display:inline-flex;
    align-items:center;
    gap:8px;
    font-family:var(--mono);
    font-size:0.74rem;
    letter-spacing:0.1em;
    text-transform:uppercase;
    color:var(--sage-dark);
    background:rgba(124,139,111,0.12);
    padding:7px 13px;
    border-radius:100px;
    margin-bottom:22px;
  }
  .eyebrow .dot{
    width:6px; height:6px;
    background:var(--sage-dark);
    border-radius:50%;
    display:inline-block;
  }
  .hero h1{
    font-family:var(--serif);
    font-weight:500;
    font-size:clamp(2.4rem, 4.4vw, 3.6rem);
    line-height:1.06;
    letter-spacing:-0.015em;
    color:var(--ink);
    margin-bottom:22px;
  }
  .hero h1 em{
    font-style:italic;
    font-weight:400;
    color:var(--clay-dark);
  }
  .hero p.lead{
    font-size:1.13rem;
    color:var(--ink-soft);
    max-width:480px;
    margin-bottom:34px;
  }
  .hero-actions{
    display:flex;
    gap:14px;
    flex-wrap:wrap;
    margin-bottom:36px;
  }
  .btn-primary{
    display:inline-flex;
    align-items:center;
    gap:10px;
    background:var(--clay);
    color:var(--white);
    padding:15px 26px;
    border-radius:8px;
    font-weight:600;
    font-size:0.98rem;
    transition:background 0.2s, transform 0.2s, box-shadow 0.2s;
    box-shadow:0 4px 14px rgba(181,85,45,0.25);
  }
  .btn-primary:hover{ background:var(--clay-dark); transform:translateY(-2px); box-shadow:0 8px 20px rgba(181,85,45,0.3); }
  .btn-secondary{
    display:inline-flex;
    align-items:center;
    gap:8px;
    background:transparent;
    color:var(--ink);
    padding:15px 22px;
    border-radius:8px;
    font-weight:600;
    font-size:0.98rem;
    border:1.5px solid var(--line);
    transition:border-color 0.2s, background 0.2s;
  }
  .btn-secondary:hover{ border-color:var(--clay); background:rgba(181,85,45,0.05); }

  .hero-meta{
    display:flex;
    gap:28px;
    flex-wrap:wrap;
  }
  .hero-meta-item{
    display:flex;
    align-items:center;
    gap:9px;
    font-size:0.88rem;
    color:var(--ink-soft);
  }
  .hero-meta-item svg{ flex-shrink:0; color:var(--sage-dark); }
  .stars{ color:var(--clay); letter-spacing:1px; font-size:0.95rem; }

  /* --- Hero visual: stylized location card, not a stock photo --- */
  .hero-visual{
    position:relative;
  }
  .locator-card{
    background:var(--white);
    border:1px solid var(--line);
    border-radius:18px;
    padding:28px;
    box-shadow:0 20px 50px -10px rgba(43,35,28,0.12);
    position:relative;
  }
  .locator-map{
    width:100%;
    aspect-ratio:4/3;
    border-radius:12px;
    position:relative;
    overflow:hidden;
    margin-bottom:18px;
    background:
      linear-gradient(135deg, var(--cream-deep) 0%, var(--cream) 100%);
  }
  .locator-map .streets{
    position:absolute;
    inset:0;
  }
  .locator-map .streets .h{
    position:absolute; left:0; right:0; height:3px; background:rgba(91,67,40,0.12);
  }
  .locator-map .streets .v{
    position:absolute; top:0; bottom:0; width:3px; background:rgba(91,67,40,0.12);
  }
  .locator-pin{
    position:absolute;
    top:50%; left:50%;
    transform:translate(-50%,-100%);
    z-index:2;
  }
  .locator-pin .pin-drop{
    width:38px; height:38px;
    background:var(--clay);
    border-radius:50% 50% 50% 0;
    transform:rotate(-45deg);
    box-shadow:0 6px 16px rgba(181,85,45,0.4);
    display:flex; align-items:center; justify-content:center;
  }
  .locator-pin .pin-drop svg{ transform:rotate(45deg); color:white; }
  .locator-pin .pin-pulse{
    position:absolute; top:38px; left:50%;
    width:14px; height:14px;
    background:rgba(181,85,45,0.35);
    border-radius:50%;
    transform:translateX(-50%);
    animation:pulse 2.2s ease-out infinite;
  }
  @keyframes pulse{
    0%{ transform:translateX(-50%) scale(0.6); opacity:0.9; }
    100%{ transform:translateX(-50%) scale(3.2); opacity:0; }
  }
  .locator-info{
    display:flex;
    justify-content:space-between;
    align-items:flex-start;
    gap:14px;
  }
  .locator-addr{
    font-family:var(--serif);
    font-size:1.05rem;
    font-weight:500;
    color:var(--ink);
    margin-bottom:4px;
  }
  .locator-sub{
    font-size:0.84rem;
    color:var(--ink-soft);
  }
  .locator-badge{
    font-family:var(--mono);
    font-size:0.7rem;
    background:rgba(124,139,111,0.14);
    color:var(--sage-dark);
    padding:6px 10px;
    border-radius:6px;
    white-space:nowrap;
    text-align:center;
  }

  @media (max-width:860px){
    .hero{ padding:48px 0 56px; }
    .hero-grid{ grid-template-columns:1fr; gap:40px; }
    .hero-visual{ order:-1; }
  }

  /* ============ SECTION LABEL SYSTEM ============ */
  .section{ padding:84px 0; }
  .section-head{
    max-width:620px;
    margin-bottom:52px;
  }
  .section-eyebrow{
    font-family:var(--mono);
    font-size:0.74rem;
    letter-spacing:0.1em;
    text-transform:uppercase;
    color:var(--clay-dark);
    margin-bottom:14px;
    display:block;
  }
  .section-head h2{
    font-family:var(--serif);
    font-weight:500;
    font-size:clamp(1.8rem, 3vw, 2.5rem);
    letter-spacing:-0.01em;
    line-height:1.15;
    color:var(--ink);
  }
  .section-head p{
    margin-top:14px;
    color:var(--ink-soft);
    font-size:1.04rem;
  }

  /* ============ TRUST STRIP ============ */
  .trust-strip{
    background:var(--cream-deep);
    border-bottom:1px solid var(--line);
    padding:22px 0;
  }
  .trust-row{
    display:flex;
    justify-content:space-between;
    align-items:center;
    flex-wrap:wrap;
    gap:18px;
  }
  .trust-item{
    display:flex;
    align-items:center;
    gap:10px;
    font-size:0.88rem;
    color:var(--ink-soft);
    font-weight:500;
  }
  .trust-item svg{ color:var(--sage-dark); flex-shrink:0; }

  /* ============ SERVICES (price ladder) ============ */
  .services-grid{
    display:flex;
    overflow-x:auto;
    scroll-behavior:smooth;
    gap:18px;
    padding-bottom:6px;
  }
  .service-card{
    min-width:280px;
    flex:1;
    border:1px solid var(--line);
    border-radius:14px;
    background:var(--white);
    padding:32px 28px;
    transition:background 0.25s;
    position:relative;
  }
  .service-card:hover{ background:var(--cream); }
  .service-icon{
    width:44px; height:44px;
    border-radius:10px;
    background:rgba(181,85,45,0.1);
    display:flex; align-items:center; justify-content:center;
    margin-bottom:18px;
    color:var(--clay-dark);
  }
  .service-card h3{
    font-family:var(--serif);
    font-weight:500;
    font-size:1.18rem;
    margin-bottom:8px;
    color:var(--ink);
  }
  .service-card p{
    font-size:0.9rem;
    color:var(--ink-soft);
    margin-bottom:18px;
    line-height:1.55;
  }
  .service-price{
    display:flex;
    align-items:baseline;
    gap:6px;
    font-family:var(--mono);
    border-top:1px dashed var(--line);
    padding-top:14px;
  }
  .service-price .from{
    font-size:0.7rem;
    color:var(--ink-soft);
    text-transform:uppercase;
    letter-spacing:0.04em;
  }
  .service-price .amount{
    font-size:1.15rem;
    font-weight:500;
    color:var(--clay-dark);
  }

  @media (max-width:860px){
    .service-card{ min-width:240px; }
  }
  @media (min-width:861px) and (max-width:1080px){
    .service-card{ min-width:260px; }
  }

  /* ============ PROCESO ============ */
  .process{
    background:var(--ink);
    color:var(--cream);
    border-radius:24px;
    padding:64px 48px;
    position:relative;
    overflow:hidden;
  }
  .process::before{
    content:'';
    position:absolute;
    top:-40%; right:-10%;
    width:520px; height:520px;
    background:radial-gradient(circle, rgba(181,85,45,0.25), transparent 70%);
    pointer-events:none;
  }
  .process-head{ position:relative; z-index:1; margin-bottom:48px; max-width:560px; }
  .process-head .section-eyebrow{ color:#E0B08C; }
  .process-head h2{ color:var(--cream); }
  .process-head p{ color:#C9BFB2; }

  .process-steps{
    display:grid;
    grid-template-columns:repeat(4, 1fr);
    gap:32px;
    position:relative;
    z-index:1;
  }
  .process-step{ position:relative; }
  .process-num{
    font-family:var(--serif);
    font-style:italic;
    font-size:2.4rem;
    color:#8A6B52;
    line-height:1;
    margin-bottom:14px;
    display:block;
  }
  .process-step h4{
    font-family:var(--sans);
    font-weight:600;
    font-size:1.02rem;
    color:var(--cream);
    margin-bottom:8px;
  }
  .process-step p{
    font-size:0.88rem;
    color:#B5AB9D;
    line-height:1.55;
  }
  .process-step:not(:last-child)::after{
    content:'';
    position:absolute;
    top:18px; right:-16px;
    width:10px; height:1px;
    background:#5C4F40;
  }

  @media (max-width:860px){
    .process{ padding:44px 26px; border-radius:18px; }
    .process-steps{ grid-template-columns:1fr 1fr; gap:36px 24px; }
    .process-step:not(:last-child)::after{ display:none; }
  }

  /* ============ TESTIMONIOS ============ */
  .testi-grid{
    display:grid;
    grid-template-columns:repeat(2, 1fr);
    gap:24px;
  }
  .testi-card{
    background:var(--white);
    border:1px solid var(--line);
    border-radius:16px;
    padding:32px;
    position:relative;
  }
  .testi-quote-mark{
    font-family:var(--serif);
    font-size:3.2rem;
    color:var(--clay);
    opacity:0.25;
    line-height:1;
    position:absolute;
    top:18px; right:24px;
  }
  .testi-stars{ color:var(--clay); font-size:0.95rem; margin-bottom:14px; letter-spacing:2px; }
  .testi-card p.quote{
    font-size:1.02rem;
    color:var(--ink);
    line-height:1.6;
    margin-bottom:22px;
    position:relative;
    z-index:1;
  }
  .testi-author{
    display:flex;
    align-items:center;
    gap:12px;
  }
  .testi-avatar{
    width:38px; height:38px;
    border-radius:50%;
    background:var(--sage);
    color:white;
    display:flex; align-items:center; justify-content:center;
    font-family:var(--serif);
    font-weight:600;
    font-size:0.95rem;
    flex-shrink:0;
  }
  .testi-name{ font-weight:600; font-size:0.92rem; color:var(--ink); }
  .testi-when{ font-size:0.8rem; color:var(--ink-soft); }

  @media (max-width:760px){
    .testi-grid{ grid-template-columns:1fr; }
  }

  /* ============ ODONTÓLOGA (perfil profesional) ============ */
  .doctor-block{
    display:grid;
    grid-template-columns:0.8fr 1.2fr;
    gap:56px;
    align-items:center;
  }
  .doctor-photo-wrap{
    position:relative;
  }
  .doctor-photo-wrap img{
    width:100%;
    aspect-ratio:4/5;
    object-fit:cover;
    border-radius:18px;
    box-shadow:0 20px 50px -10px rgba(43,35,28,0.18);
  }
  .doctor-photo-tag{
    position:absolute;
    bottom:-18px;
    left:24px;
    background:var(--white);
    border:1px solid var(--line);
    border-radius:10px;
    padding:12px 18px;
    box-shadow:0 10px 24px -6px rgba(43,35,28,0.14);
  }
  .doctor-photo-tag .role{
    font-family:var(--mono);
    font-size:0.68rem;
    letter-spacing:0.06em;
    text-transform:uppercase;
    color:var(--sage-dark);
  }
  .doctor-photo-tag .name{
    font-family:var(--serif);
    font-weight:500;
    font-size:1rem;
    color:var(--ink);
  }
  .doctor-info .section-eyebrow{ color:var(--clay-dark); }
  .doctor-info h2{
    font-family:var(--serif);
    font-weight:500;
    font-size:clamp(1.8rem, 3vw, 2.5rem);
    letter-spacing:-0.01em;
    line-height:1.15;
    color:var(--ink);
    margin-bottom:16px;
  }
  .doctor-credentials{
    display:flex;
    flex-direction:column;
    gap:14px;
    margin-top:28px;
  }
  .doctor-credential{
    display:flex;
    align-items:flex-start;
    gap:14px;
    padding:16px 18px;
    background:var(--white);
    border:1px solid var(--line);
    border-radius:12px;
  }
  .doctor-credential-icon{
    width:34px; height:34px;
    flex-shrink:0;
    border-radius:8px;
    background:rgba(181,85,45,0.1);
    display:flex; align-items:center; justify-content:center;
    color:var(--clay-dark);
  }
  .doctor-credential p{
    font-size:0.95rem;
    color:var(--ink);
    font-weight:500;
    line-height:1.45;
  }

  @media (max-width:860px){
    .doctor-block{ grid-template-columns:1fr; gap:40px; }
    .doctor-photo-wrap{ max-width:340px; margin:0 auto; }
  }

  /* ============ UBICACIÓN / FOOTER CTA ============ */
  .location-block{
    background:var(--cream-deep);
    border-radius:24px;
    padding:0;
    overflow:hidden;
    display:grid;
    grid-template-columns:1fr 1fr;
  }
  .location-info{
    padding:52px 48px;
  }
  .location-info .section-eyebrow{ color:var(--clay-dark); }
  .location-info h2{
    font-family:var(--serif);
    font-weight:500;
    font-size:2rem;
    margin-bottom:18px;
    color:var(--ink);
  }
  .location-list{
    display:flex;
    flex-direction:column;
    gap:18px;
    margin:28px 0 32px;
  }
  .location-row{
    display:flex;
    gap:14px;
    align-items:flex-start;
  }
  .location-row svg{ color:var(--clay-dark); flex-shrink:0; margin-top:2px; }
  .location-row .label{ font-weight:600; font-size:0.92rem; color:var(--ink); margin-bottom:2px; }
  .location-row .val{ font-size:0.9rem; color:var(--ink-soft); }
  .location-row .val .status{
    color:var(--sage-dark);
    font-weight:600;
  }

  .location-visual{
    position:relative;
    background:linear-gradient(150deg, #DDD0BC 0%, #EFE5D5 60%);
    overflow:hidden;
  }
  .location-visual .streets-big .h{ position:absolute; left:0; right:0; height:4px; background:rgba(91,67,40,0.10); }
  .location-visual .streets-big .v{ position:absolute; top:0; bottom:0; width:4px; background:rgba(91,67,40,0.10); }
  .location-visual .building{
    position:absolute;
    width:120px; height:90px;
    background:repeating-linear-gradient(0deg, #C76B3F, #C76B3F 9px, #B5552D 9px, #B5552D 18px);
    border-radius:3px;
    box-shadow:0 8px 22px rgba(43,35,28,0.18);
  }
  .location-visual .pin-big{
    position:absolute;
    transform:translate(-50%,-100%);
    z-index:3;
  }
  .location-visual .pin-big .pin-drop{
    width:52px; height:52px;
    background:var(--clay);
    border-radius:50% 50% 50% 0;
    transform:rotate(-45deg);
    box-shadow:0 10px 24px rgba(181,85,45,0.45);
    display:flex; align-items:center; justify-content:center;
    border:3px solid var(--cream);
  }
  .location-visual .pin-big .pin-drop svg{ transform:rotate(45deg); color:white; width:22px; height:22px; }

  @media (max-width:860px){
    .location-block{ grid-template-columns:1fr; }
    .location-visual{ min-height:280px; }
    .location-info{ padding:40px 28px; }
  }

  /* ============ FAQ ============ */
  .faq-list{
    border-top:1px solid var(--line);
  }
  .faq-item{
    border-bottom:1px solid var(--line);
  }
  .faq-q{
    width:100%;
    display:flex;
    justify-content:space-between;
    align-items:center;
    gap:20px;
    padding:24px 4px;
    background:none;
    border:none;
    text-align:left;
    font-family:var(--sans);
    font-size:1.04rem;
    font-weight:600;
    color:var(--ink);
    cursor:pointer;
  }
  .faq-q .icon{
    flex-shrink:0;
    width:22px; height:22px;
    border-radius:50%;
    border:1.5px solid var(--clay);
    color:var(--clay-dark);
    display:flex; align-items:center; justify-content:center;
    transition:transform 0.3s;
    font-size:0.9rem;
  }
  .faq-item[data-open="true"] .faq-q .icon{ transform:rotate(45deg); }
  .faq-a{
    max-height:0;
    overflow:hidden;
    transition:max-height 0.35s ease;
  }
  .faq-item[data-open="true"] .faq-a{ max-height:220px; }
  .faq-a p{
    padding:0 4px 24px;
    color:var(--ink-soft);
    font-size:0.95rem;
    max-width:680px;
  }

  /* ============ FOOTER ============ */
  footer{
    background:var(--ink);
    color:#B5AB9D;
    padding:48px 0 28px;
  }
  .footer-grid{
    display:flex;
    justify-content:space-between;
    align-items:flex-start;
    flex-wrap:wrap;
    gap:32px;
    padding-bottom:32px;
    border-bottom:1px solid #443A2E;
  }
  .footer-brand .brand-mark{ color:var(--cream); }
  .footer-brand p{ font-size:0.86rem; margin-top:10px; max-width:280px; color:#8F8576; }
  .footer-col h5{
    font-family:var(--mono);
    font-size:0.7rem;
    letter-spacing:0.08em;
    text-transform:uppercase;
    color:#8F8576;
    margin-bottom:14px;
  }
  .footer-col a, .footer-col p{
    display:block;
    font-size:0.88rem;
    color:#C9BFB2;
    margin-bottom:9px;
  }
  .footer-col a:hover{ color:var(--clay); }
  .footer-bottom{
    padding-top:22px;
    display:flex;
    justify-content:space-between;
    flex-wrap:wrap;
    gap:10px;
    font-size:0.8rem;
    color:#766C5E;
  }

  /* ============ STICKY MOBILE CTA ============ */
  .mobile-cta{
    display:none;
  }
  @media (max-width:640px){
    .mobile-cta{
      display:flex;
      position:fixed;
      bottom:0; left:0; right:0;
      z-index:60;
      background:var(--white);
      border-top:1px solid var(--line);
      padding:12px 16px;
      box-shadow:0 -8px 24px rgba(43,35,28,0.08);
    }
    .mobile-cta a{
      flex:1;
      background:var(--clay);
      color:white;
      text-align:center;
      padding:13px;
      border-radius:8px;
      font-weight:600;
      font-size:0.95rem;
    }
    body{ padding-bottom:74px; }
  }

</style>

<style>
.gallery-section{padding:100px 20px;background:#f7fbfb}
.container{max-width:1200px;margin:auto}
.section-header{text-align:center;margin-bottom:50px}
.gallery-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:18px}
.gallery-item{overflow:hidden;border-radius:18px;cursor:pointer;box-shadow:0 8px 20px rgba(0,0,0,.08)}
.gallery-item img{width:100%;height:280px;object-fit:cover;transition:.4s}
.gallery-item:hover img{transform:scale(1.05)}
.gallery-wide{grid-column:span 3}
.gallery-wide img{height:380px}
.lightbox{display:none;position:fixed;inset:0;background:rgba(0,0,0,.92);z-index:9999;justify-content:center;align-items:center}
.lightbox img{max-width:90%;max-height:90%}
.close-lightbox{position:absolute;top:20px;right:30px;color:white;font-size:48px;cursor:pointer}
</style>
</head>
<body>

<header>
  <div class="nav">
    <div class="brand">
      <span class="brand-mark">Irigoyen</span>
      <span class="brand-sub">Odontología</span>
    </div>
    <nav class="nav-links">
      <a href="#tratamientos">Tratamientos</a>
      <a href="#odontologa">Nuestra odontóloga</a>
      <a href="#proceso">Cómo es tu visita</a>
      <a href="#galeria">Galería</a>
      <a href="#opiniones">Opiniones</a>
      <a href="#faq">Preguntas</a>
      <a href="#ubicacion">Ubicación</a>
    </nav>
    <a class="nav-cta" href="https://wa.me/5491168317733?text=Hola%2C%20quiero%20sacar%20un%20turno%20en%20Odontolog%C3%ADa%20Irigoyen" target="_blank" rel="noopener">
      Turno por WhatsApp
    </a>
  </div>
</header>

<!-- ============ HERO ============ -->
<section class="hero">
  <div class="brick-texture" aria-hidden="true"></div>
  <div class="wrap hero-grid">
    <div>
      <span class="eyebrow"><span class="dot"></span> Villa Devoto · Irigoyen 2551</span>
      <h1>Tu dentista de <em>confianza</em>, con la calidez de siempre.</h1>
      <p class="lead">Atención personalizada, puntual y prolija — sin vueltas. Consultas, extracciones, implantes y más, a cinco minutos de tu casa.</p>
      <div class="hero-actions">
        <a class="btn-primary" href="https://wa.me/5491168317733?text=Hola%2C%20quiero%20sacar%20un%20turno%20en%20Odontolog%C3%ADa%20Irigoyen" target="_blank" rel="noopener">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor"><path d="M12 2C6.48 2 2 6.48 2 12c0 1.85.5 3.58 1.37 5.07L2 22l5.07-1.32A9.94 9.94 0 0012 22c5.52 0 10-4.48 10-10S17.52 2 12 2zm0 18c-1.65 0-3.18-.45-4.5-1.23l-.32-.19-3.34.87.89-3.25-.21-.34A7.95 7.95 0 014 12c0-4.41 3.59-8 8-8s8 3.59 8 8-3.59 8-8 8zm4.4-5.6c-.24-.12-1.43-.7-1.65-.78-.22-.08-.38-.12-.54.12-.16.24-.62.78-.76.94-.14.16-.28.18-.52.06-.24-.12-1.01-.37-1.92-1.18-.71-.63-1.19-1.41-1.33-1.65-.14-.24-.01-.37.11-.49.12-.12.24-.28.36-.42.12-.14.16-.24.24-.4.08-.16.04-.3-.04-.42-.08-.12-.62-1.49-.85-2.04-.22-.53-.45-.46-.62-.47-.16-.01-.34-.01-.52-.01-.18 0-.46.07-.7.32-.24.25-.93.91-.93 2.2 0 1.3.95 2.55 1.08 2.73.13.18 1.81 2.77 4.4 3.77 2.59 1 2.59.66 3.06.62.46-.04 1.43-.58 1.63-1.15.2-.56.2-1.04.14-1.15-.06-.1-.22-.16-.46-.28z"/></svg>
          Reservar turno
        </a>
        <a class="btn-secondary" href="#tratamientos">
          Ver tratamientos y precios
        </a>
      </div>
      <div class="hero-meta">
        <div class="hero-meta-item">
          <span class="stars">★★★★★</span> 5.0 en Google
        </div>
        <div class="hero-meta-item">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><path d="M12 6v6l4 2"/></svg>
          Lun a sáb · turnos puntuales
        </div>
        <div class="hero-meta-item">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 21s-7-5.33-7-11a7 7 0 1114 0c0 5.67-7 11-7 11z"/><circle cx="12" cy="10" r="2.5"/></svg>
          A metros del subte
        </div>
      </div>
    </div>

    <div class="hero-visual">
      <div class="locator-card">
        <div class="locator-map">
          <div class="streets">
            <div class="h" style="top:32%"></div>
            <div class="h" style="top:68%"></div>
            <div class="v" style="left:38%"></div>
            <div class="v" style="left:74%"></div>
          </div>
          <div class="locator-pin">
            <div class="pin-drop">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M19 21l-2-2H7l-2 2V8a5 5 0 0110 0v13z"/><path d="M9 8h6"/></svg>
            </div>
            <div class="pin-pulse"></div>
          </div>
        </div>
        <div class="locator-info">
          <div>
            <div class="locator-addr">Irigoyen 2551</div>
            <div class="locator-sub">Villa Devoto, CABA</div>
          </div>
          <div class="locator-badge">Abre 10&nbsp;a.m.<br>lun a sáb</div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ============ TRUST STRIP ============ -->
<div class="trust-strip">
  <div class="wrap trust-row">
    <div class="trust-item">
      <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M9 12l2 2 4-4"/><circle cx="12" cy="12" r="10"/></svg>
      Atención puntual, sin esperas largas
    </div>
    <div class="trust-item">
      <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M20.84 4.61a5.5 5.5 0 00-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 10-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 000-7.78z"/></svg>
      Espacio cuidado y amigable con LGBTQ+
    </div>
    <div class="trust-item">
      <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M3 21h18M5 21V7l8-4 8 4v14M9 9h1m4 0h1m-6 4h1m4 0h1m-6 4h1m4 0h1"/></svg>
      Consultorio dirigido por una empresaria local
    </div>
  </div>
</div>

<!-- ============ TRATAMIENTOS ============ -->
<section class="section" id="tratamientos">
  <div class="wrap">
    <div class="section-head">
      <span class="section-eyebrow">Tratamientos</span>
      <h2>Atención general y especializada, con precios claros.</h2>
      <p>Sin sorpresas: te contamos el valor estimado de cada tratamiento desde la primera consulta.</p>
    </div>
    <div class="services-grid">
      <div class="service-card">
        <div class="service-icon">
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M9 12l2 2 4-4"/><circle cx="12" cy="12" r="10"/></svg>
        </div>
        <h3>Consulta general</h3>
        <p>Revisión completa, diagnóstico y plan de tratamiento explicado en lenguaje claro.</p>
        <div class="service-price"><span class="from">Desde</span> <span class="amount">$15.000</span></div>
      </div>
      <div class="service-card">
        <div class="service-icon">
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="18" height="18" rx="2"/><path d="M9 9h6v6H9z"/></svg>
        </div>
        <h3>Radiografía</h3>
        <p>Imágenes digitales para un diagnóstico preciso antes de cualquier tratamiento.</p>
        <div class="service-price"><span class="from">Desde</span> <span class="amount">$20.000</span></div>
      </div>
      <div class="service-card">
        <div class="service-icon">
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 2v20M5 8l7-6 7 6M5 16l7 6 7-6"/></svg>
        </div>
        <h3>Extracciones</h3>
        <p>Procedimientos cuidadosos, con anestesia local y seguimiento post-extracción.</p>
        <div class="service-price"><span class="from">Desde</span> <span class="amount">$60.000</span></div>
      </div>
      <div class="service-card">
        <div class="service-icon">
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 21s-7-5.33-7-11a7 7 0 1114 0c0 5.67-7 11-7 11z"/><circle cx="12" cy="10" r="2.5"/></svg>
        </div>
        <h3>Tratamiento de conducto</h3>
        <p>Solución duradera para dientes con caries profundas o infección de la pulpa.</p>
        <div class="service-price"><span class="from">Desde</span> <span class="amount">$140.000</span></div>
      </div>
      <div class="service-card">
        <div class="service-icon">
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M13 2L3 14h7l-1 8 10-12h-7l1-8z"/></svg>
        </div>
        <h3>Terapia láser</h3>
        <p>Tratamiento de encías y tejidos blandos, con recuperación más rápida.</p>
        <div class="service-price"><span class="from">Desde</span> <span class="amount">$35.000</span> <span class="from">/ sesión</span></div>
      </div>
      <div class="service-card">
        <div class="service-icon">
          <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 2L2 7l10 5 10-5-10-5z"/><path d="M2 17l10 5 10-5M2 12l10 5 10-5"/></svg>
        </div>
        <h3>Implantes dentales</h3>
        <p>Reemplazo fijo y natural para piezas perdidas, con seguimiento completo.</p>
        <div class="service-price"><span class="from">Desde</span> <span class="amount">$600.000</span></div>
      </div>
    </div>
  </div>
</section>

<!-- ============ ODONTÓLOGA ============ -->
<section class="section" id="odontologa">
  <div class="wrap">
    <div class="doctor-block">
      <div class="doctor-photo-wrap">
        <img src="foto1.jpg" alt="Odontóloga de Odontología Irigoyen">
        <div class="doctor-photo-tag">
          <div class="role">Odontóloga</div>
          <div class="name">Odontología Irigoyen</div>
        </div>
      </div>

      <div class="doctor-info">
        <span class="section-eyebrow">Conocé a nuestra odontóloga</span>
        <h2>Formación y experiencia detrás de cada sonrisa.</h2>
        <p>Un equipo formado y actualizado, para que cada tratamiento esté respaldado por conocimiento real.</p>

        <div class="doctor-credentials">
          <div class="doctor-credential">
            <div class="doctor-credential-icon">
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M22 10v6M2 10l10-5 10 5-10 5z"/><path d="M6 12v5c3 3 9 3 12 0v-5"/></svg>
            </div>
            <p>Odontología recibida en Universidad de Buenos Aires</p>
          </div>
          <div class="doctor-credential">
            <div class="doctor-credential-icon">
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 2a10 10 0 100 20 10 10 0 000-20z"/><path d="M12 6v6l4 2"/></svg>
            </div>
            <p>Especialista en ortopedia y ortodoncia</p>
          </div>
          <div class="doctor-credential">
            <div class="doctor-credential-icon">
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 2v4M12 18v4M4.93 4.93l2.83 2.83M16.24 16.24l2.83 2.83M2 12h4M18 12h4M4.93 19.07l2.83-2.83M16.24 7.76l2.83-2.83"/></svg>
            </div>
            <p>Diplomatura en Láser con aplicaciones en Odontología</p>
          </div>
          <div class="doctor-credential">
            <div class="doctor-credential-icon">
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="8" r="5"/><path d="M20 21a8 8 0 10-16 0"/></svg>
            </div>
            <p>Diplomatura en Armonización Facial</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ============ PROCESO ============ -->
<section class="section" id="proceso">
  <div class="wrap">
    <div class="process">
      <div class="process-head">
        <span class="section-eyebrow">Cómo es tu visita</span>
        <h2>De la consulta al resultado, sin sorpresas.</h2>
        <p>Así se ve, paso a paso, tu primera vez en Odontología Irigoyen.</p>
      </div>
      <div class="process-steps">
        <div class="process-step">
          <span class="process-num">01</span>
          <h4>Coordinás por WhatsApp</h4>
          <p>Nos escribís, te proponemos día y horario. Respuesta rápida, sin esperas.</p>
        </div>
        <div class="process-step">
          <span class="process-num">02</span>
          <h4>Revisión y diagnóstico</h4>
          <p>Te explicamos qué encontramos y qué opciones tenés, en lenguaje claro.</p>
        </div>
        <div class="process-step">
          <span class="process-num">03</span>
          <h4>Plan a tu medida</h4>
          <p>Definimos juntos los pasos a seguir y el valor estimado de cada uno.</p>
        </div>
        <div class="process-step">
          <span class="process-num">04</span>
          <h4>Tratamiento y seguimiento</h4>
          <p>Te acompañamos en cada sesión hasta el resultado final.</p>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ============ GALERÍA ============ -->
<section class="gallery-section" id="galeria">
<div class="container">
<div class="section-header">
<span>Nuestras instalaciones</span>
<h2>Conocé nuestro consultorio</h2>
<p>Imágenes reales del consultorio, sala de espera y frente del local.</p>
</div>

<div class="gallery-grid">
<div class="gallery-item"><img src="image-1.png" alt="Frente del consultorio Odontología Irigoyen"></div>
<div class="gallery-item"><img src="image-2.png" alt="Sala de espera del consultorio"></div>
<div class="gallery-item"><img src="image-3.png" alt="Sillón odontológico y equipo de atención"></div>
<div class="gallery-item"><img src="image-4.png" alt="Sala de tratamiento equipada"></div>
<div class="gallery-item"><img src="image-5.png" alt="Recepción del consultorio"></div>
<div class="gallery-item"><img src="image-6.png" alt="Detalle de instrumental odontológico"></div>
<div class="gallery-item gallery-wide"><img src="image-7.png" alt="Vista general del consultorio"></div>
</div>
</div>
</section>

<div id="lightbox" class="lightbox">
<span class="close-lightbox" role="button" aria-label="Cerrar imagen" tabindex="0">&times;</span>
<img id="lightbox-img" alt="">
</div>

<!-- ============ TESTIMONIOS ============ -->
<section class="section" id="opiniones">
  <div class="wrap">
    <div class="section-head">
      <span class="section-eyebrow">Opiniones</span>
      <h2>Lo que dicen quienes ya vinieron.</h2>
      <p>5.0 sobre 5 en Google — reseñas reales de pacientes de Villa Devoto y alrededores.</p>
    </div>

    <div class="testi-grid">
      <div class="testi-card">
        <span class="testi-quote-mark">"</span>
        <div class="testi-stars">★★★★★</div>
        <p class="quote">Muy buen servicio! Me sentí muy cómoda durante todo el tratamiento, fueron puntuales en la atención y el espacio está muy bien cuidado.</p>
        <div class="testi-author">
          <div class="testi-avatar">J</div>
          <div>
            <div class="testi-name">Juana Di Tieri</div>
            <div class="testi-when">Hace 3 meses</div>
          </div>
        </div>
      </div>

      <div class="testi-card">
        <span class="testi-quote-mark">"</span>
        <div class="testi-stars">★★★★★</div>
        <p class="quote">Muy buen servicio, me sentí muy bien atendido, 100% recomendado.</p>
        <div class="testi-author">
          <div class="testi-avatar">R</div>
          <div>
            <div class="testi-name">Ramiro Passarini</div>
            <div class="testi-when">Hace 3 meses · 7 opiniones en Google</div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>


<!-- ============ UBICACIÓN ============ -->
<section class="section" id="ubicacion">
  <div class="wrap">
    <div class="location-block">
      <div class="location-info">
        <span class="section-eyebrow">Ubicación y horarios</span>
        <h2>Encontranos en Villa Devoto.</h2>

        <div class="location-list">
          <div class="location-row">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M19 21l-2-2H7l-2 2V8a5 5 0 0110 0v13z"/><path d="M9 8h6"/></svg>
            <div>
              <div class="label">Dirección</div>
              <div class="val">Irigoyen 2551, C1408, Cdad. Autónoma de Buenos Aires</div>
            </div>
          </div>
          <div class="location-row">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><path d="M12 6v6l4 2"/></svg>
            <div>
              <div class="label">Horario</div>
              <div class="val">Lunes a sábado, desde las <span class="status">10:00 hs</span></div>
            </div>
          </div>
          <div class="location-row">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M22 16.92v3a2 2 0 01-2.18 2 19.79 19.79 0 01-8.63-3.07 19.5 19.5 0 01-6-6 19.79 19.79 0 01-3.07-8.67A2 2 0 014.11 2h3a2 2 0 012 1.72c.13.96.36 1.9.68 2.81a2 2 0 01-.45 2.11L8.09 9.91a16 16 0 006 6l1.27-1.27a2 2 0 012.11-.45c.91.32 1.85.55 2.81.68A2 2 0 0122 16.92z"/></svg>
            <div>
              <div class="label">Teléfono / WhatsApp</div>
              <div class="val">011 15-6831-7733</div>
            </div>
          </div>
        </div>

        <a class="btn-primary" href="https://wa.me/5491168317733?text=Hola%2C%20quiero%20sacar%20un%20turno%20en%20Odontolog%C3%ADa%20Irigoyen" target="_blank" rel="noopener">
          Escribinos por WhatsApp
        </a>
      </div>

      <div class="location-visual">
        <div class="streets-big">
          <div class="h" style="top:28%"></div>
          <div class="h" style="top:64%"></div>
          <div class="v" style="left:42%"></div>
          <div class="v" style="left:78%"></div>
        </div>
        <div class="building" style="top:34%; left:30%;"></div>
        <div class="pin-big" style="top:34%; left:42%;">
          <div class="pin-drop">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M19 21l-2-2H7l-2 2V8a5 5 0 0110 0v13z"/><path d="M9 8h6"/></svg>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- ============ FAQ ============ -->
<section class="section" id="faq">
  <div class="wrap">
    <div class="section-head">
      <span class="section-eyebrow">Preguntas frecuentes</span>
      <h2>Antes de escribirnos, quizás esto ya responde tu duda.</h2>
    </div>

    <div class="faq-list">
      <div class="faq-item" data-open="false">
        <button class="faq-q" onclick="toggleFaq(this)" aria-expanded="false">
          ¿Necesito sacar turno o puedo venir directamente?
          <span class="icon">+</span>
        </button>
        <div class="faq-a">
          <p>Recomendamos coordinar por WhatsApp para asegurarte un horario, pero si estás cerca y tenés una urgencia, escribinos y vemos cómo podemos ayudarte el mismo día.</p>
        </div>
      </div>
      <div class="faq-item" data-open="false">
        <button class="faq-q" onclick="toggleFaq(this)" aria-expanded="false">
          ¿Atienden urgencias dentales?
          <span class="icon">+</span>
        </button>
        <div class="faq-a">
          <p>Sí. Si tenés dolor agudo, un golpe o se te rompió una pieza, escribinos por WhatsApp y te damos prioridad dentro de nuestro horario de atención.</p>
        </div>
      </div>
      <div class="faq-item" data-open="false">
        <button class="faq-q" onclick="toggleFaq(this)" aria-expanded="false">
          ¿Qué medios de pago aceptan?
          <span class="icon">+</span>
        </button>
        <div class="faq-a">
          <p>Aceptamos efectivo y transferencia. Para tratamientos de mayor valor, como implantes o conductos, podemos coordinar el pago en cuotas según el caso.</p>
        </div>
      </div>
      <div class="faq-item" data-open="false">
        <button class="faq-q" onclick="toggleFaq(this)" aria-expanded="false">
          ¿Trabajan con alguna obra social o prepaga?
          <span class="icon">+</span>
        </button>
        <div class="faq-a">
          <p>Por el momento la atención es particular, sin convenio directo con obras sociales o prepagas. Te damos el comprobante para que puedas presentarlo si tu cobertura reconoce reintegros.</p>
        </div>
      </div>
      <div class="faq-item" data-open="false">
        <button class="faq-q" onclick="toggleFaq(this)" aria-expanded="false">
          ¿Cuánto tarda en estar listo un implante?
          <span class="icon">+</span>
        </button>
        <div class="faq-a">
          <p>Depende de cada caso, pero en general el proceso completo —desde la colocación hasta la pieza definitiva— lleva entre 3 y 6 meses. En la primera consulta te damos un cronograma estimado.</p>
        </div>
      </div>
    </div>
  </div>
</section>

<footer>
  <div class="wrap">
    <div class="footer-grid">
      <div class="footer-brand">
        <span class="brand-mark">Irigoyen</span>
        <p>Odontología de barrio en Villa Devoto. Atención cercana, prolija y puntual desde Irigoyen 2551.</p>
      </div>
      <div class="footer-col">
        <h5>Contacto</h5>
        <a href="https://wa.me/5491168317733">011 15-6831-7733</a>
        <a href="#ubicacion">Irigoyen 2551, CABA</a>
      </div>
      <div class="footer-col">
        <h5>Navegación</h5>
        <a href="#tratamientos">Tratamientos</a>
        <a href="#odontologa">Nuestra odontóloga</a>
        <a href="#proceso">Cómo es tu visita</a>
        <a href="#galeria">Galería</a>
        <a href="#opiniones">Opiniones</a>
      </div>
      <div class="footer-col">
        <h5>Horario</h5>
        <p>Lunes a sábado</p>
        <p>Desde las 10:00 hs</p>
      </div>
    </div>
    <div class="footer-bottom">
      <span>© 2026 Odontología Irigoyen</span>
      <span>Hecho con cuidado, en Villa Devoto</span>
    </div>
  </div>
</footer>

<div class="mobile-cta">
  <a href="https://wa.me/5491168317733?text=Hola%2C%20quiero%20sacar%20un%20turno" target="_blank" rel="noopener">Reservar turno por WhatsApp</a>
</div>

<script>
  function toggleFaq(btn){
    const item = btn.closest('.faq-item');
    const isOpen = item.getAttribute('data-open') === 'true';
    document.querySelectorAll('.faq-item').forEach(i => {
      i.setAttribute('data-open','false');
      i.querySelector('.faq-q').setAttribute('aria-expanded','false');
    });
    const nowOpen = !isOpen;
    item.setAttribute('data-open', nowOpen ? 'true' : 'false');
    btn.setAttribute('aria-expanded', nowOpen ? 'true' : 'false');
  }
</script>

<script>
  const lightbox = document.getElementById('lightbox');
  const lightboxImg = document.getElementById('lightbox-img');
  const closeLightbox = document.querySelector('.close-lightbox');

  function openLightbox(img){
    lightbox.style.display = 'flex';
    lightboxImg.src = img.src;
    lightboxImg.alt = img.alt;
  }
  function closeLightboxFn(){
    lightbox.style.display = 'none';
  }

  document.querySelectorAll('.gallery-item img').forEach(img=>{
    img.addEventListener('click', () => openLightbox(img));
  });
  lightbox.addEventListener('click', closeLightboxFn);
  closeLightbox.addEventListener('keydown', e => {
    if(e.key === 'Enter' || e.key === ' ') closeLightboxFn();
  });
  document.addEventListener('keydown', e => {
    if(e.key === 'Escape') closeLightboxFn();
  });
</script>

</body>
</html>
